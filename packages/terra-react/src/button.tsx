import type * as React from "react"

import {
  Button,
  buttonVariants,
} from "../../../apps/v4/styles/base-nova/ui/button"

type ButtonProps = React.ComponentProps<typeof Button>

export { Button, buttonVariants, type ButtonProps }
